<style>
  .taichi-text__bg-gradient {
    @apply h-screen;
    background: linear-gradient(45deg, #000 0, #000 50%, #fff 50%);
  }

  .taichi-text__bg-gradient::before {
    @apply absolute text-white text-[100px];
    content: 'This is a text effect make with mix-blend-mode:difference;';
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    mix-blend-mode: difference;
    animation: move 5s infinite linear alternate;
  }

  @keyframes move {
    0% {
      transform: translate(-30%, -50%);
    }
    100% {
      transform: translate(-70%, -50%);
    }
  }
</style>

<div class='taichi-text__bg-gradient'></div>
